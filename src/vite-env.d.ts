/// <reference types="vite/client" />

// Build stamp injected by vite.config (define) so the running app can show which
// build it is — makes "did the new version actually deploy?" verifiable on-screen.
declare const __BUILD__: string;
